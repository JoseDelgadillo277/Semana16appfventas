{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter pub get && ./flutter/bin/flutter build web --release --dart-define=CORE_BASE_URL=$CORE_BASE_URL",
  "outputDirectory": "build/web"
}
